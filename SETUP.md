# 🚀 Настройка окружения для разработки PaceUp

## ✅ Что уже установлено и настроено

- ✅ **Flutter 3.38.6** — установлен и настроен
- ✅ **Android SDK 36.1.0** — установлен и настроен
- ✅ **Xcode 26.2** — установлен и настроен
- ✅ **CocoaPods 1.16.2** — установлен с Ruby 3.3.0
- ✅ **iOS зависимости** — установлены через CocoaPods
- ✅ **Android зависимости** — готовы к использованию
- ✅ **Flutter зависимости** — установлены (`flutter pub get`)

## 📱 Доступные эмуляторы

### Android эмуляторы
- **Medium Phone API 36.1** — готов к использованию

### iOS симуляторы
- **iPhone 17 Pro** и другие модели iOS 26.2 — готовы к использованию

## 🎯 Быстрый старт

### 1. Запуск Android эмулятора

```bash
# Просмотр доступных эмуляторов
flutter emulators

# Запуск Android эмулятора
flutter emulators --launch Medium_Phone_API_36.1

# Или через Android Studio:
# Tools → Device Manager → запустить эмулятор
```

### 2. Запуск iOS симулятора

```bash
# Способ 1: Открыть симулятор через команду
open -a Simulator

# Способ 2: Запустить конкретное устройство по ID
xcrun simctl boot "EE9C712B-C785-4602-8042-1F20290A2A62"  # iPhone 17 Pro

# Способ 3: Через Xcode
# Xcode → Open Developer Tool → Simulator → выбрать устройство

# После запуска симулятора проверьте доступные устройства
flutter devices
```

### 3. Запуск на реальный iPhone

Для тестирования приложения на физическом iPhone необходимо настроить подпись кода (code signing):

#### Шаг 1: Подготовка Apple Developer аккаунта

1. **Бесплатный аккаунт (для личного тестирования):**
   - Откройте Xcode → Settings → Accounts
   - Добавьте свой Apple ID (или используйте существующий)
   - Выберите аккаунт → нажмите "Download Manual Profiles" (если нужно)

2. **Платный аккаунт (для публикации в App Store):**
   - Требуется подписка Apple Developer Program ($99/год)
   - Дает больше возможностей и возможность публикации

#### Шаг 2: Настройка подписи в Xcode

```bash
# Откройте проект в Xcode
cd /Users/alexeyfomin/Documents/paceup/app/paceup
open ios/Runner.xcworkspace
```

В Xcode:
1. Выберите проект **Runner** в навигаторе слева
2. Выберите target **Runner**
3. Перейдите на вкладку **Signing & Capabilities**
4. Включите **"Automatically manage signing"**
5. Выберите свою **Team** (ваш Apple ID или Developer Team)
6. Xcode автоматически создаст provisioning profile и сертификат

**Важно:** Bundle Identifier должен быть уникальным. Текущий: `com.example.paceup`
- Если нужно изменить: в поле **Bundle Identifier** введите уникальный ID (например, `com.yourname.paceup`)

#### Шаг 3: Подключение iPhone

1. Подключите iPhone к Mac через USB кабель
2. На iPhone: **Настройки → Основные → VPN и управление устройством**
3. Разблокируйте iPhone и доверьтесь компьютеру (если появится запрос)
4. На iPhone: **Настройки → Конфиденциальность и безопасность → Режим разработчика** (включите, если доступно)

#### Шаг 4: Проверка подключения

```bash
# Проверьте, что iPhone виден в системе
flutter devices

# Должно показать что-то вроде:
# iPhone (mobile) • 00008030-001A... • ios • iOS 17.0
```

#### Шаг 5: Запуск приложения на iPhone

```bash
# Перейти в директорию проекта
cd /Users/alexeyfomin/Documents/paceup/app/paceup

# Запуск на подключенном iPhone
flutter run

# Или указать конкретное устройство
flutter devices  # посмотреть список устройств
flutter run -d <device-id>  # запустить на конкретном устройстве
```

**При первом запуске:**
- На iPhone появится запрос о доверии к сертификату разработчика
- Перейдите: **Настройки → Основные → VPN и управление устройством**
- Найдите профиль разработчика и нажмите **"Доверять"**
- Перезапустите приложение

#### Альтернативный способ через Xcode

1. Откройте `ios/Runner.xcworkspace` в Xcode
2. Выберите ваш iPhone в списке устройств (вверху рядом с кнопкой Run)
3. Нажмите кнопку **Run** (▶️) или `Cmd + R`
4. Приложение соберется и установится на iPhone

### 4. Запуск приложения

```bash
# Перейти в директорию проекта
cd /Users/alexeyfomin/Documents/paceup/app/paceup

# Запуск на Android (сначала запустите эмулятор)
flutter emulators --launch Medium_Phone_API_36.1
flutter run

# Запуск на iOS симуляторе (сначала откройте симулятор)
open -a Simulator
# Подождите несколько секунд, пока симулятор загрузится
flutter devices  # проверьте доступные устройства
flutter run -d EE9C712B-C785-4602-8042-1F20290A2A62  # iPhone 17 Pro
# Или просто:
flutter run  # Flutter выберет первый доступный iOS симулятор

# Запуск на реальном iPhone (см. раздел выше)
flutter run  # если iPhone подключен, Flutter выберет его автоматически
```

## 🔧 Дополнительные команды

### Проверка окружения
```bash
flutter doctor -v
```

### Обновление зависимостей
```bash
# Flutter зависимости
flutter pub get

# iOS зависимости (если изменился Podfile)
cd ios && pod install && cd ..
```

### Очистка проекта
```bash
# Очистка build файлов
flutter clean

# Пересборка зависимостей
flutter pub get
```

## ⚙️ Конфигурация проекта

### API настройки
- **Base URL**: `https://api.paceup.ru` (настроен в `lib/core/config/app_config.dart`)
- **Backend**: PHP/MySQL на удаленном сервере (не требует локальной настройки)

### Firebase
- **Android**: `android/app/src/debug/google-services.json` — настроен
- **iOS**: требуется добавить `GoogleService-Info.plist` в `ios/Runner/` (если еще не добавлен)

### MapBox
- **Access Token**: настроен в `lib/core/config/app_config.dart`
- **MapTiler API Key**: настроен в `lib/core/config/app_config.dart`

## 🐛 Решение проблем

### Проблема: CocoaPods не работает
```bash
# Убедитесь, что используется правильная версия Ruby
eval "$(rbenv init - zsh)"
ruby --version  # должно быть 3.3.0

# Переустановите зависимости
cd ios
pod deintegrate
pod install
cd ..
```

### Проблема: Android эмулятор не запускается
```bash
# Проверьте, что Android SDK установлен
echo $ANDROID_HOME

# Создайте новый эмулятор через Android Studio:
# Tools → Device Manager → Create Device
```

### ✅ Решено: Firebase Messaging временно отключен для iOS
```bash
# Firebase Messaging временно отключен для iOS из-за проблем с модульными заголовками
# FCM работает только на Android
# iOS сборка теперь проходит успешно без ошибок

# Для включения FCM на iOS в будущем:
# 1. Обновите firebase_messaging до версии 16.0.0+
# 2. Уберите проверки Platform.isIOS из кода
# 3. Обновите зависимости и пересоберите проект
```

### ✅ Решено: Приложение падает при запуске из-за HealthKit
```bash
# Проблема: Приложение падало с ошибкой HealthKit при запуске
# Причина: Отсутствовали обязательные ключи в Info.plist

# Решение: Добавлены ключи в ios/Runner/Info.plist:
# - NSHealthShareUsageDescription (для чтения данных)
# - NSHealthUpdateUsageDescription (для записи данных)

# Теперь приложение запускается без ошибок
```

### Проблема: iOS симулятор не запускается или не виден в Flutter
```bash
# 1. Проверьте, что Xcode установлен
xcode-select --print-path

# 2. Установите командные инструменты (если нужно)
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer

# 3. Откройте симулятор вручную
open -a Simulator

# 4. Запустите конкретное устройство
xcrun simctl list devices available  # показать все доступные устройства
xcrun simctl boot "EE9C712B-C785-4602-8042-1F20290A2A62"  # запустить по ID

# 5. Проверьте, что устройство видно в Flutter
flutter devices

# 6. Если устройство все еще не видно, перезапустите Flutter daemon
flutter daemon --stop
flutter devices
```

### Проблема: "No supported devices found" при запуске iOS
```bash
# Убедитесь, что симулятор запущен и загружен
open -a Simulator
# Подождите 5-10 секунд, пока симулятор полностью загрузится
flutter devices  # проверьте список устройств
flutter run  # запустите без указания конкретного устройства
```

### Проблема: iPhone не определяется Flutter
```bash
# 1. Проверьте подключение через USB
# Убедитесь, что кабель поддерживает передачу данных (не только зарядку)

# 2. Проверьте, что устройство разблокировано и доверено компьютеру
# На iPhone: Настройки → Основные → VPN и управление устройством

# 3. Перезапустите Flutter daemon
flutter daemon --stop
flutter devices

# 4. Проверьте через Xcode
open ios/Runner.xcworkspace
# В Xcode: Window → Devices and Simulators
# Убедитесь, что iPhone виден в списке устройств

# 5. Если iPhone не виден в Xcode:
# - Отключите и подключите кабель заново
# - Перезагрузите iPhone
# - Проверьте, что на iPhone включен режим разработчика (iOS 16+)
```

### Проблема: Ошибка подписи кода (Code Signing Error)
```bash
# 1. Откройте проект в Xcode
open ios/Runner.xcworkspace

# 2. В Xcode:
# - Выберите Runner в навигаторе
# - Target: Runner → Signing & Capabilities
# - Включите "Automatically manage signing"
# - Выберите правильную Team
# - Убедитесь, что Bundle Identifier уникален

# 3. Очистите и пересоберите проект
cd /Users/alexeyfomin/Documents/paceup/app/paceup
flutter clean
flutter pub get
cd ios && pod install && cd ..
flutter build ios --debug

# 4. Если проблема сохраняется:
# - Удалите старые provisioning profiles:
#   ~/Library/MobileDevice/Provisioning Profiles/
# - В Xcode: Product → Clean Build Folder (Cmd + Shift + K)
# - Попробуйте снова
```

### Проблема: "Untrusted Developer" на iPhone
```bash
# После установки приложения на iPhone:
# 1. На iPhone: Настройки → Основные → VPN и управление устройством
# 2. Найдите профиль разработчика (ваше имя или название команды)
# 3. Нажмите на профиль → "Доверять [ваше имя]"
# 4. Подтвердите доверие
# 5. Перезапустите приложение на iPhone
```

### Проблема: Приложение устанавливается, но не запускается
```bash
# 1. Проверьте логи на iPhone через Xcode:
# Xcode → Window → Devices and Simulators
# Выберите iPhone → View Device Logs

# 2. Проверьте, что все разрешения настроены в Info.plist
# Особенно важно для этого проекта:
# - NSHealthShareUsageDescription
# - NSHealthUpdateUsageDescription
# - NSContactsUsageDescription

# 3. Убедитесь, что версия iOS на iPhone >= 14.0 (IPHONEOS_DEPLOYMENT_TARGET)

# 4. Пересоберите приложение:
flutter clean
flutter pub get
cd ios && pod install && cd ..
flutter run
```

## 📝 Полезные ссылки

- [Flutter документация](https://docs.flutter.dev/)
- [Android Studio](https://developer.android.com/studio)
- [Xcode](https://developer.apple.com/xcode/)
- [CocoaPods](https://cocoapods.org/)

## 🎉 Готово к разработке!

Все необходимое для работы с проектом PaceUp установлено и настроено. Можете запускать эмуляторы и начинать разработку!
